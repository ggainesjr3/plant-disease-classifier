{
  "name": "plant-disease-classifier-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": ". venv/bin/activate && python3 -u backend/app.py",
    "frontend": "cd frontend && npm run dev",
    "dev:all": "concurrently --kill-others \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "lucide-react": "^1.8.0"
  }
}